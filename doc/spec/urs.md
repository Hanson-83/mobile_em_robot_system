# 用户需求规范（URS）— 移动环境监测机器人上位机软件系统

| 项 | 内容 |
|----|------|
| 文档编号 | MER-URS-001 |
| 版本 | V0.3 |
| 日期 | 2026-09-25 |
| 作者 | PM_Max |
| 状态 | Draft |
| 范围 | **仅上位机（服务器 + 客户端）软件**；硬件设备选型与采购由用户负责 |
| 依据 | `ref/requirement.md`；对齐 `spec/tech_stack.md`、`spec/interface_contracts.md`、`spec/security.md`、`spec/testing_quality.md` |
| 语言 | 中文 |

---

## 1. 范围与边界

### 1.1 范围声明

本 URS **仅覆盖上位机软件系统**（服务器端 + 多类型客户端）。AMR、监测仪表、充电桩、电梯硬件、机械臂、采样机构、现场网络基建等由用户自行选型与交付；上位机通过**适配层契约**接入用户选定并保证可用的外部设备 API/协议。

### 1.2 In Scope（上位机）

- **服务器端软件**：任务/订单下发；原子技能与组合/集群任务编排；多机调度（MVP 简单互斥）；状态与地图可视化后端；数据采集持久化；实时/历史趋势；点位管理；限值与报警；断点续传接收；报警事件；批报告；多用户/用户组/权限/安全策略；可开关审计追踪与电子签名（开启时强制批准流）；备份与灾难恢复；设备驱动适配层（抽象接口 + 配置装配，**不含具体硬件选型**）；**API Gateway**（对外统一入口）；多客户端后端与契约；电梯联动软件技能与 Fake。
- **客户端**：Vue Web（MVP）优先；桌面/移动可分期；均经 API Gateway 访问。
- **与已由用户选定的 AMR/仪表的软件集成契约**：API/协议适配器接口、Fake/Simulator、配置 schema、错误模型、坐标系/单位约定（具体型号字段留占位/配置驱动）。

### 1.3 Out of Scope / 用户负责

- AMR 厂家选型与商务、机械改装、充电桩硬件、电梯硬件改造。
- 粒子/温湿度/风速/浮游菌等仪表选型与采购；机械臂选型；采样机构机械设计；现场网络基建选型。
- 地面吸尘、拖地等清洁功能（**本系统不做清洁机**）。
- 正式合规认证/证书出具（如完整 IQ/OQ/PQ、第三方 Part 11 认证）— 本 URS 仅要求**取向与可开关能力**，认证范围 TBD。
- 选型清单、改装 BOM、他厂硬件参数对照作为选型结论的内容（文档中不作为本项目交付）。

### 1.4 分期（软件）

| 阶段 | 上位机交付重点 |
|------|----------------|
| MVP | 适配层 + Fake（含电梯技能 Fake）；任务编排与多机简单互斥；地图/状态可视化；粒子/温湿度/风速**数据模型与采样命令**；报警；批报告；RBAC；审计/签名开关（开启则批准流）；备份；**API Gateway** + OpenAPI；Vue Web 客户端（MVP） |
| 扩展 | 浮游菌等仪表适配契约扩展；更丰富编排与看板；桌面/移动客户端增强；交通管制增强 |
| 远期 | 协作臂/深度相机**软件技能与适配接口**（硬件由用户提供）；轨迹采样编排；更优多机交通算法 |

---

## 2. 干系人与使用场景

| 角色 | 典型诉求 |
|------|----------|
| 洁净室/QA 运营 | 按计划自动巡检，数据可追溯，报警及时 |
| 设施/EHS | 温湿度、风速、粒子超限告警与趋势 |
| 制药验证/合规（如适用） | 审计追踪、电子签名可开关（开启进批准流），数据完整性取向 |
| 系统集成/运维 | 适配多品牌设备（配置驱动）、备份恢复、日志可观测 |
| 开发/测试 | Fake/Simulator、OpenAPI、分层测试 |
| 管理层 | 多机效率、批报告、看板 |
| 第三方系统（MES/SCADA/DCS 等） | 经 API Gateway 调用常规操作、数据获取、设置类接口 |
| 用户（硬件 Owner） | 提供已选定设备的 API/协议文档与联调窗口 |

**主场景（MVP）**：运营在上位机编排「区域 A→B→C」监测任务 → 经适配层调度机器人导航至点位 → 触发仪表采样命令 → 数据上传与可视化 → 超限报警 → 任务结束生成批报告；跨楼层时走电梯联动技能（Fake 可验收，真机依赖用户设备）。

**扩展场景**：浮游菌采样任务编排；断网缓存后续传接收。

**远期场景**：经臂适配接口编排定点/轨迹采样（硬件就绪为前提）。

---

## 3. 术语

| 术语 | 说明 |
|------|------|
| AMR | Autonomous Mobile Robot，自主移动机器人（本 URS 中为**外部系统**） |
| 上位机 | 中央编排调度与数据中控软件（服务器 + 客户端）— **本 URS 交付主体** |
| 原子技能 | 不可再分的可执行动作（如导航到点、开始粒子采样） |
| 任务 | 由一组原子技能组成的可调度单元 |
| 组合任务 | 由多个任务按顺序/条件组成的工作流 |
| 集群任务 | 多机器人协同或并行的任务编排 |
| 适配层 | **对内**设备接入层：隔离业务与具体设备协议的驱动适配框架（MVP 直连仪表；未来可扩展边端代理） |
| 边端 | 通俗定义：部署在**机器人本体/车载工控**上的代理软件（**非 MVP**；未来可选）。若采用，须具备端侧数据缓存与断网续传能力 |
| 直连 | 仪表**独立联网**，由上位机适配层直接通讯（不经边端代理）— **MVP 采用** |
| API Gateway | **对外**统一入口：第三方系统与前端 App 均经此网关调用上位机能力（与对内适配层职责分离） |
| Fake/Simulator | 无硬件环境下的设备模拟实现 |
| 外部系统 | 用户选型并交付的 AMR/仪表/电梯等，经适配契约接入 |
| 批准流 | 与电子签名绑定的审批流程；签名开启时强制进入 |
| Part 11 | 21 CFR Part 11 取向（可开关能力，非已获证声明） |
| TBD | 待确认 |

---

## 4. 用户需求明细

优先级：`Must` / `Should` / `Could`。阶段：`MVP` / `扩展` / `远期`。

### 4.1 机器人软件能力与外部依赖假设

> 本节描述上位机须具备的**软件能力**，以及对用户交付机器人系统的**假设/依赖**。硬件能力本身不在本 URS 交付范围。

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-ROB-001 | 上位机须通过适配层接入符合契约的 AMR（型号由用户提供/配置） | Must | YAML/配置可注册机器人；切换 Fake 与真实适配器无需改编排核心 | MVP |
| URS-ROB-002 | 上位机支持点位配置/管理、导航到点指令下发、位姿与状态订阅可视化 | Must | 可配置监测点；`navigate_to` 后状态机与地图位姿更新正确（真机依赖用户 AMR API） | MVP |
| URS-ROB-003 | 上位机支持充电/回充相关状态展示与 `dock_charge` 类技能调度 | Must | 低电量策略可配置触发；充电状态可从适配层上报并展示 | MVP |
| URS-ROB-004 | 适配层须覆盖连接、心跳、导航、取消、状态/遥测订阅等 AMR 契约方法 | Must | 接口清单有文档；Fake 与至少一种真实/录制适配器可跑通 | MVP |
| URS-ROB-005 | 上位机与 AMR（及直连设备）的通信会话可监测；断线有明确错误与重连策略 | Must | 离线产生事件；重连后任务可恢复或明确失败（见数据续传） | MVP |
| URS-ROB-006 | （依赖）用户保证机器人供电与仪表供电满足现场任务；上位机仅处理上报的供电/急停相关状态字段（若契约提供） | Should | 契约字段可配置映射；缺失字段不崩溃 | MVP |
| URS-ROB-007 | **电梯联动纳入 MVP 验收**：接口层 Call/Enter/Exit 技能与任务编排集成；须提供 Fake；真机联调依赖用户电梯硬件/协议就绪 | Must | 技能与状态机存在；Fake 可演完整电梯联动编排；MVP 软件验收不依赖真机，但不可省略软件能力与 Fake | MVP |
| URS-ROB-008 | （依赖）洁净室材质/等级由用户与 AMR 供应商保证；上位机不验收硬件材质 | — | 记入外部假设，非软件验收项 | — |
| URS-ROB-009 | 上位机可接收并展示急停/故障类安全状态（字段由契约定义） | Must | 急停/故障状态触发报警或任务中断策略可测（Fake 可注入） | MVP |
| URS-ROB-010 | 扩展仪表（如浮游菌）以适配器 + 配置扩展，不改调度核心 | Should | 新增适配器类型可注册并出现在技能目录 | 扩展 |
| URS-ROB-011 | 远期预留 ArmAdapter / DepthCameraAdapter 接口与技能占位 | Could | 接口与 Fake 骨架存在；非 MVP 阻塞 | 远期 |
| URS-ROB-012 | 远期支持臂定点/轨迹采样编排演示（依赖用户臂硬件与 API） | Could | Fake 演示：移动 → 臂到位 → 采样完成 | 远期 |

**外部依赖（用户保证）**：所选 AMR 提供可用的 SLAM/导航/充电/电梯（若现场需要跨楼层）API 或等价协议；地图可导出或可同步至上位机显示层。电梯硬件/品牌由用户负责。

### 4.2 监测仪表 — 软件集成

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-INS-001 | 支持粒子计数器采样控制命令与粒径通道数据模型（型号由用户配置） | Must | 适配层可 `start_sample` / `stop_sample` / `read_channels`；数据带时间戳与点位 ID | MVP |
| URS-INS-002 | 支持温湿度读数命令与数据模型（型号由用户配置） | Must | 温度、湿度写入时序存储；单位与精度在配置中声明 | MVP |
| URS-INS-003 | 支持风速读数命令与数据模型（型号由用户配置） | Must | 风速数据可读并可关联点位；姿态/高度属外部机械假设 | MVP |
| URS-INS-004 | 仪表通信经适配层，协议细节不泄漏到业务编排 | Must | 业务仅调用领域命令；协议细节仅存适配层与配置 | MVP |
| URS-INS-005 | 仪表型号可配置替换（YAML），新增型号以适配器+配置为主 | Must | 切换 Fake 与真实/录制适配器无需改编排核心 | MVP |
| URS-INS-006 | 扩展：浮游菌采集器采样参数下发与任务关联记录 | Should | 可下发时长/流量等**配置化参数**并落库 | 扩展 |
| URS-INS-007 | 仪表计量校准状态可登记（校准到期提醒可选） | Could | 可录入校准日期/证书编号；到期提醒可配置 | 扩展 |
| URS-INS-008 | **MVP 对内拓扑为直连**：仪表（及适用设备）独立联网，由上位机适配层直接通讯，不上边端代理 | Must | 适配层直连 Fake/真实路径可测；配置与验收不以边端代理为前提 | MVP |
| URS-INS-009 | （演进）预留未来边端代理模式：适配层接口可扩展；边端代理**必须**支持端侧数据缓存（断网/弱网本地缓存，恢复后续传） | Should | 接口/配置有代理模式占位；端侧缓存与续传契约有文档；非 MVP 验收阻塞 | 扩展 |

> 具体仪表型号、通讯参数由用户在配置中提供；文档不给出选型结论。
> **对内拓扑决议**：MVP/现在 = **直连**（不上边端）；未来再考虑边端代理（须含端侧缓存）。演进：直连 → 可选边端代理。勿将两种模式写成对等默认。

### 4.3 调度编排

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-SCH-001 | 可下发订单/任务，调度机器人前往区域/点位执行监测 | Must | UI/API 创建任务后状态迁移正确直至完成或失败 | MVP |
| URS-SCH-002 | 原子技能可组合为任务；任务可组合为组合任务（含等待时间/事件） | Must | 示例流程可执行并留痕 | MVP |
| URS-SCH-003 | 支持多机器人各自任务与集群任务编排；**MVP 采用简单互斥**（资源锁/区域互斥）；更优交通管制留待后续迭代 | Must | ≥2 台 Fake/仿真并行；点位/电梯/充电桩等逻辑资源互斥可测；文档标明演进路径 | MVP |
| URS-SCH-004 | 实时监测机器人位置与状态并可视化 | Must | 地图上位姿刷新；状态含空闲/执行/充电/故障等 | MVP |
| URS-SCH-005 | 地图可视化：载入地图、点位、机器人移动轨迹/位姿 | Must | 可导入/同步 AMR 地图或等价表示；点位可增删改 | MVP |
| URS-SCH-006 | （演进）多机路径冲突更优交通管制/算法 | Could | 在简单互斥之上增强；不阻塞 MVP | 扩展/远期 |

### 4.4 数据采集与可视化

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-DAT-001 | 采集并持久化粒子、温湿度、风速等监测数据 | Must | 任务关联可查询；字段含机器人、点位、时间、数值、单位 | MVP |
| URS-DAT-002 | 实时数据、动态趋势、历史数据与历史趋势可视化 | Must | 看板可看实时与可选时间窗历史曲线 | MVP |
| URS-DAT-003 | 监测/采样点位管理（名称、坐标/地图关联、仪表类型、限值） | Must | CRUD 与任务引用一致；删除有引用保护 | MVP |
| URS-DAT-004 | 极限设置与超限检测 | Must | 超限产生报警事件（见 URS-ALM） | MVP |
| URS-DAT-005 | 断点续传**接收**（幂等）：MVP 由上位机/适配层侧重试与幂等入库；未来边端端侧缓存补传亦须可接收 | Must | 模拟断网补传无错误重复（幂等键）；不以边端代理为 MVP 前提 | MVP |

### 4.5 报警与事件

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-ALM-001 | 实时报警与事件消息（超限、设备离线、任务失败等） | Must | 客户端可实时收到；含级别、来源、时间、关联对象 | MVP |
| URS-ALM-002 | 异常处理流程（确认、关闭、备注） | Should | 报警生命周期可追踪；权限控制谁可关闭 | MVP |
| URS-ALM-003 | 报警/事件历史查询与导出 | Must | 可按时间/类型过滤；可导出（格式 TBD，如 CSV/Excel） | MVP |

### 4.6 批报告

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-RPT-001 | 任务完成后自动生成批报告（单任务） | Must | 含任务元数据、点位结果摘要、超限项、执行时间线 | MVP |
| URS-RPT-002 | 组合任务与集群任务报告 | Should | 报告可汇总子任务结果 | MVP |
| URS-RPT-003 | 报告可下载/归档；格式 PDF 或等价（TBD） | Should | 文件可追溯到任务 ID | MVP |

### 4.7 权限与安全

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-SEC-001 | 多用户、多用户组、可配置权限 | Must | 角色至少区分查看/操作/管理；无权限操作被拒绝 | MVP |
| URS-SEC-002 | 安全策略可配置（密码策略、会话超时等，细则 TBD） | Should | 策略变更生效并可审计（若审计开启） | MVP |
| URS-SEC-003 | 经 API Gateway 的对外/客户端 API 需鉴权；密钥不入库明文 | Must | 未鉴权请求失败；密钥走环境变量/密钥通道 | MVP |

### 4.8 审计追踪与电子签名

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-AUD-001 | 审计追踪功能可开启/关闭 | Must | 开启后关键操作留痕：谁/何时/何对象/何变更 | MVP |
| URS-AUD-002 | 电子签名功能可配置开启或禁用；**开启时必须进入批准流**（批准流与签名绑定） | Must | 配置项可开/关；开启后对指定动作（如报告批准、限值变更）须完成签名+批准流后方可生效；关闭时不阻塞主流程 | MVP |
| URS-AUD-003 | 审计记录防篡改取向（追加写、权限限制删除） | Should | 普通用户不可删除审计；管理员删除若允许则本身被审计（策略细则可后续细化） | MVP |
| URS-AUD-004 | （建议，非硬性）电子签名**默认关闭**；按客户/现场需要开启 | Should | 出厂/默认配置 `features.e_sign: false`；文档标明为建议默认 | MVP |

> 体现 **21 CFR Part 11 / 数据完整性取向**，不声称已获合规证书。
> 用户决议：需要能设置开启或禁用；若开启则必须进批准流。建议默认关闭（标为建议，非用户硬性要求）。

### 4.9 备份与恢复

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-BAK-001 | 支持自动与手动备份（配置、业务库、报告文件等范围 TBD） | Must | 可成功生成备份集并记录时间 | MVP |
| URS-BAK-002 | 支持从备份恢复 | Must | 测试环境完成一次恢复演练并校验关键数据可读 | MVP |

### 4.10 设备适配层（对内）

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-ADP-001 | 通过适配层适配不同仪表与机器人（抽象接口 + 配置装配） | Must | 抽象接口 + 至少 Fake + 一种真实/录制实现路径 | MVP |
| URS-ADP-002 | 适配层封装连接、心跳、超时、重连、编解码 | Must | 断线重连可测；超时可区分可重试/不可重试 | MVP |
| URS-ADP-003 | 提供 Fake/Simulator 供无硬件开发与 CI（含电梯 Fake） | Must | CI 无真机可跑通关键编排冒烟（含电梯技能路径） | MVP |
| URS-ADP-004 | 适配层与 API Gateway 职责分离：适配层=对内设备接入；Gateway=对外统一入口 | Must | 架构图与模块边界清晰；客户端/第三方不直连适配器 | MVP |

### 4.11 多客户端

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-CLI-001 | 支持 Vue Web 客户端（MVP）；经 API Gateway 访问 | Must | 主流现代浏览器可完成核心操作 | MVP |
| URS-CLI-002 | 桌面端 App | Could | 基于同一 API Gateway；技术选型 TBD | 扩展 |
| URS-CLI-003 | 移动端 App | Could | 优先只读监控/报警；完整操作能力 TBD | 扩展 |

### 4.12 对外 API（API Gateway）

| ID | 描述 | 优先级 | 验收标准 | 阶段 |
|----|------|--------|----------|------|
| URS-API-001 | 上位机对外须有 **API Gateway**，提供统一接口与 OpenAPI 文档 | Must | 第三方与前端均经 Gateway；有 OpenAPI；不绕过 Gateway 直连内部服务 | MVP |
| URS-API-002 | 统一错误模型（码、消息、可重试标记） | Must | 错误响应符合约定；版本策略文档化 | MVP |
| URS-API-003 | 首批对接：第三方系统（高层 MES、SCADA、DCS 等）与前端 App（Vue Web 及后续移动/桌面） | Must | 文档列出对接对象；能力覆盖常规操作、数据获取、设置类接口 | MVP |
| URS-API-004 | Gateway 能力范围：常规操作、数据获取、设置类接口 | Must | OpenAPI 中可见对应分组；鉴权与权限码对齐 RBAC | MVP |

---

## 5. 非功能需求

| ID | 类别 | 描述 | 优先级 | 验收/备注 |
|----|------|------|--------|-----------|
| URS-NFR-001 | 性能 | 遥测与看板刷新延迟目标 TBD（试点基线后冻结） | Should | 软件侧可测指标与现场网络基线分开记录 |
| URS-NFR-002 | 可靠性 | 上位机关键服务可重启恢复；任务状态机可恢复 | Must | 杀进程后任务状态不丢失或可明确失败 |
| URS-NFR-003 | 安全 | 对齐 `spec/security.md`：密钥外置、最小权限、默认鉴权 | Must | 安全检查清单通过 |
| URS-NFR-004 | 可维护 | 单体模块化；配置 YAML；日志结构化 | Must | 目录与配置评审通过 |
| URS-NFR-005 | 外部约束 | 洁净室速度/宽度/材质等属用户现场与硬件范围 | — | 非上位机软件验收项；可作集成假设备注 |
| URS-NFR-006 | 可测试 | 分层测试；HIL 默认跳过 CI | Must | 见 `plan.md`；主路径 Fake（含电梯） |
| URS-NFR-007 | 部署 | 服务器优先 Ubuntu；Docker Compose 可选 | Should | 安装文档可复现 |

---

## 6. 假设与依赖（外部系统）

1. **用户负责** AMR/仪表/充电桩/电梯硬件/网络基建的选型、采购、改装与现场就绪。
2. 用户提供所选设备的 **API/协议文档、联调环境或仿真器**；适配器按文档实现。
3. 用户保证机器人具备（或将具备）SLAM/导航/充电/电梯（跨楼层场景）等能力的可调用接口。
4. 现场 WiFi/VLAN 由用户 IT 提供；上位机侧实现断线检测与续传接收。
5. MVP 可不启用审计/电子签名即可完成主流程验收（**建议默认关闭签名**）；开启签名时强制批准流；制药试点按客户需要打开开关。
6. 他厂参考方案仅可借鉴**上位机侧**看板/数据上抛/任务流程思路；不引导本项目做清洁机或硬件选型。
7. 电梯真机联调依赖用户设备就绪；MVP 软件验收以技能 + Fake 为准。

---

## 7. 开放问题

**暂无阻塞性开放问题。** 下列为轻微 TBD（不阻塞 MVP 主路径）：

1. 报告导出具体格式（PDF/其他）与报警导出格式细节。
2. 时序存储选型（PostgreSQL 分区表 vs Timescale 等）。
3. 地图中间表示标准化细节、时区展示策略、备份范围细目、密码策略细则。
4. 桌面/移动客户端技术选型（扩展阶段）。
5. 多机交通管制增强算法形态（扩展/远期，MVP 已定简单互斥）。

> 硬件选型类问题由用户硬件 Owner 闭环。下列上位机决策已于 2026-09-25 关闭：电子签名开/关+批准流、API Gateway 与首批对接、对内拓扑 MVP 直连、边端为演进（须端侧缓存）、电梯纳入 MVP、多机简单互斥及演进路径。

---

## 8. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| V0.1 | 2026-09-25 | 初稿，依据 requirement 生成 |
| V0.2 | 2026-09-25 | 范围收窄为上位机软件；硬件改为外部依赖/假设；开放问题改为上位机决策项 |
| V0.2.1 | 2026-09-25 | 用户确认前端锁定 Vue（TypeScript + Vue）；客户端改为 Vue Web（MVP），关闭前端选型问题 |
| V0.3 | 2026-09-25 | 写入软件决策：电子签名可配+开启强制批准流（建议默认关）；API Gateway 与首批对接；对内拓扑 MVP 直连、边端为演进（须端侧缓存）；电梯纳入 MVP；多机简单互斥及演进；开放问题大幅收敛 |
