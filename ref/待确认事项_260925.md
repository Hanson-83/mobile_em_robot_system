# 待确认事项（实现阶段）

日期：2026-09-25。下列项已按设计文档做了可运行的默认选择，或明确占位。请确认是否维持。真机资料齐了之后再替换占位适配器。

| ID | 事项 | 当前做法 | 需要你确认 |
|----|------|----------|------------|
| C-01 | 开发种子账号与 API Token | `admin/Admin123!`、`operator/Operator1!`、`viewer/Viewer123!`，Token `dev-api-token-change-me`。仅 `MER_DEV_SEED=true` | 生产初始管理员如何发放 |
| C-02 | 低电量回充阈值 | 20% | 是否改默认值 |
| C-03 | 口令与会话 | 最短 8 位；会话 480 分钟 | 试点后再冻结或现在改 |
| C-04 | 地图格式 | 只接受标准化 JSON（宽高、点位、米制局部坐标） | 请后续提供 AMR 厂商地图样例 |
| C-05 | 真机协议 | `amr_vendor_x` / `elevator_vendor_x` / 仪表厂商适配器会拒绝连接 | 请提供 AMR、电梯、粒子、温湿度、风速 API |
| C-06 | 报告格式 | HTML 归档，无 PDF | 是否指定 weasyprint 或浏览器打印 |
| C-07 | 时序库 | 开发/CI 为 SQLite；PostgreSQL 月分区脚本在 `deploy/sql/`，未自动执行 | 试点库是否直接上 PostgreSQL |
| C-08 | 进程重启 | 在途任务标记失败 `PROCESS_RESTART`，不自动接着跑 | 是否改为重新排队 |
| C-09 | 断线 | 默认任务失败，不恢复原任务 | 是否改为等待重连后继续 |
| C-10 | 传输加密 | 按方案默认内网 HTTP + 鉴权 | 是否现在就要 TLS |
| C-11 | 批准流 | 单步单人；发起人可以自己批 | 是否要求另一人审批 |
| C-12 | 互斥范围 | 点位、电梯、充电桩、区域。没有「一台机器人同时只跑一个任务」的锁 | 是否要加机器人锁 |
| C-13 | 报警关闭 | 必须先确认再关闭 | 是否允许直接关闭 |
| C-14 | 边端代理 | 配置可写 `via_edge_agent`，没有端侧缓存实现 | 维持扩展项即可 |
| C-15 | 浮游菌 / 机械臂 / 桌面 / 移动 | 仅占位或未做 | 维持不进本次 MVP |
| C-16 | PostgreSQL 备份 | 应用内自动备份覆盖 SQLite 文件、活动 YAML 与报告目录。生产 PostgreSQL 仍需你方 `pg_dump` | 生产备份是否由你方执行 `pg_dump` |
| C-17 | 任务状态机 | MVP 创建即 `Queued`，拿到锁即 `Running`，没有单独的 Created / Dispatched | 是否要补上这两个中间状态 |
| C-18 | 权限生效时机 | 角色或用户组变更后，已登录会话仍用旧权限，重新登录才更新 | 是否要改成立即失效 |
| C-19 | 自动备份间隔 | 默认 60 分钟，进程启动时若到期会做一次。设为 0 关闭 | 试点间隔要不要改 |

真机测试（TC-HIL-01/02/03）请你在专用环境执行，结果可回填 `doc/test.md`。
