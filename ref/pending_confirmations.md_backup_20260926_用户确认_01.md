# 待用户确认清单（资料不全 / 真机依赖）

更新日期：2026-09-25  
原则：不阻塞 Fake 主路径；下列项用占位或模拟继续开发。真机测试由用户在专门环境执行后回填 `doc/test.md`。

| ID | 主题 | 当前处理 | 需要用户确认/补充 |
|----|------|----------|-------------------|
| Q-01 | AMR 厂商 HTTP/SDK 契约（导航、位姿、地图同步、急停、充电） | `AmrFake` + `AmrVendorX` 占位 | API 文档、鉴权方式、坐标系/单位 |
| Q-02 | 电梯/楼控 API（Call/Enter/Exit、楼层、门状态） | `ElevatorFake` + `ElevatorVendorX` 占位 | 协议、楼层约定、是否经 AMR 聚合（设计已锁定独立 ElevatorAdapter） |
| Q-03 | 粒子/温湿度/风速仪表协议（HTTP/Modbus/串口等） | 三类 Fake；直连 YAML | 型号、通道定义、启停/读数帧、单位 |
| Q-04 | AMR 地图文件格式与导入方式 | `/api/v1/maps` 占位一条 | 厂工具导出格式、图层、点位对齐 |
| Q-05 | 生产用户目录 / LDAP/OIDC | 开发账号 admin/operator | 是否保持本地账号到试点；密码策略数值 |
| Q-06 | 现场 TLS | 默认内网 HTTP + 强制鉴权 | 是否书面接受明文；或强制 TLS |
| Q-07 | 报告 PDF 引擎 | HTML 归档为默认方向 | weasyprint vs chromium |
| Q-08 | 时序库 | MVP PG 分区（未建表） | 保留期与 QPS 是否已有试点目标 |
| Q-09 | MES/SCADA/DCS 首批字段 | Gateway 分组草图 | 对端系统与必选接口清单 |
| Q-10 | 低电量回充阈值与触发归属 | `dock_charge` Fake 有；策略未编 | 阈值%、由调度还是车体自管 |
| Q-11 | 会话断线默认策略 | 设计默认 Failed | 是否改为等待重连 N 秒 |
| Q-12 | 互斥冲突默认 queue 还是 fail | M1 可测 fail；queue 待 M2 | 试点现场偏好 |
| Q-13 | 时区与展示 | UTC 日志 | 现场时区 |
| Q-14 | 充电桩/区域资源 ID 规则 | 锁类型已预留 | ID 命名与地图绑定 |

硬件选型、采购、电梯改造、真机窗口：**不在本清单实现范围内**，由用户硬件 Owner 安排；软件侧仅等 API 与联调反馈。
