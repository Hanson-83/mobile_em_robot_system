# 待用户确认清单（不阻塞 Fake 主路径）

| 更新日期 | 2026-09-25 |
| 规则 | 不确定项已用占位/Fake 继续开发；请逐项确认或后续补资料 |

| ID | 主题 | 当前处理 | 需要你确认/补充的内容 | 阻塞？ |
|----|------|----------|------------------------|--------|
| Q-01 | 真实 AMR HTTP API | `amr_vendor_x` 占位，装配即拒绝 | 导航/状态/充电/地图同步接口文档、鉴权、坐标系 | 仅阻塞 M6 真机 |
| Q-02 | 真实仪表协议 | `particle/climate/airflow_vendor_x` 占位 | 粒子/温湿度/风速远程启停与读数协议、单位 | 仅阻塞 M6 |
| Q-03 | 真实电梯/楼控 API | `elevator_vendor_x` 占位 | Call/Enter/Exit 或等价协议、楼层模型 | 仅阻塞电梯真机 |
| Q-04 | 地图中间表示 | 前端占位画布 + `maps` 示例 | AMR 厂地图导出格式、显示层标准化规则 | 不阻塞 Fake |
| Q-05 | 报告 PDF 引擎 | 默认 HTML 归档 | weasyprint 或 chromium 二选一 | 不阻塞 |
| Q-06 | 密码/会话数值 | `password_min_len=8`，`session_ttl_min=480` | 是否采纳或给出现场策略 | 不阻塞 |
| Q-07 | 低电量回充阈值 | `low_battery_pct: null` | 默认百分比及是否由调度触发 | 不阻塞 |
| Q-08 | 时序库 | MVP 用内存→M3 PG 分区 | 是否按 DS §5.2 门槛再评估 Timescale | 不阻塞 |
| Q-09 | MES/SCADA/DCS 字段 | OpenAPI 草图已分组 | 首批对接系统清单与必填字段 | 不阻塞 M1–M4 |
| Q-10 | 开发默认口令 | `admin/admin`（可用环境变量覆盖） | 是否要求改出厂口令策略 | 部署前建议确认 |
| Q-11 | 资源锁 queue | M1 冲突立即返回；M2 做等待队列 | 确认 MVP 冲突策略默认 `queue` 是否仍成立 | 不阻塞 |
| Q-12 | 真机联调窗口 | HIL 跳过 CI | 设备就绪时间、实验室环境、反馈格式 | 仅 M6 |

硬件选型、电梯品牌、现场网络仍由你（硬件 Owner）负责，不在本清单展开。
