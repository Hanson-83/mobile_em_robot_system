# M3 数据与合规代码审核意见（2026-09-26）

## 1. 审核元信息

| 项 | 内容 |
|----|------|
| 审核对象 | M3：点位/限值/趋势、RBAC、审计开关、电子签名批准流、备份恢复 |
| 仓库 / 分支 | `/workspace` / `cursor/m3-compliance-8317` |
| 审核提交 | `471a834` — `feat(m3): 点位限值、批准流、审计与备份恢复` |
| 审核依据 | `doc/spec/ds.md` §6.2、§9、§10.2；`ref/plan.md` TC-MVP-01/06/07/09/13a/b/c/17/21 |
| 审核范围 | `backend/app/services/compliance.py`、`backup.py`、`store.py`；`backend/app/api/routes/compliance.py`；`backend/tests/test_m3_compliance.py` 及关联合规路径 |
| 审核人 | **Reviewer_Cmp** |
| 审核日期 | 2026-09-26 |
| 总体结论 | **有条件通过** |

结论说明：批准流主路径（`e_sign=false` 直接生效、`true` 时 409 待批、审批人≠发起人、口令挑战、驳回/过期不落限值）、审计 API 层禁止删除、备份含 SQLite 库 + `config/` YAML + 报告目录且 **DB 摘要篡改时拒绝恢复**，自动化 **36 passed**。与 DS/URS 仍存在 **限值变更仅驻内存不写配置/库**、**点位 PATCH 可绕过签名**、**恢复后进程内限值/配置未热加载** 等 P1 偏差；审计开关与备份 manifest 校验覆盖偏弱（P2）。建议 **闭环 P1 后再进 M4**，P2 可与 M4 并行。

## 2. 验证结果

| 检查 | 结果 |
|------|------|
| 后端测试：`cd /workspace/backend && .venv/bin/pytest -q` | **通过**：36 passed，1 warning（Starlette/anyio 弃用） |
| 审核提交 | 与分支 HEAD `471a834` 一致 |
| 密钥不入库 | `Store` 无口令/secret 字段；`MER_SECRET` / `MER_DEV_USERS_JSON` 经环境注入（`security.py`、`config.validate_settings`） |

## 3. 与计划用例对照（TC）

| 用例 | 结论 | 证据 / 缺口 |
|------|------|-------------|
| TC-MVP-01 点位与限值 | **部分符合** | `test_point_crud_and_operator_forbidden` 覆盖点位 CRUD；全局限值经 `/settings/limits`；**点位级 `limits` 未与批准流绑定**（见 M3-002） |
| TC-MVP-06 RBAC 越权 | **基本符合** | operator 改全局限值 403（M3 + `test_gateway.test_rbac_operator_forbidden_settings`）；点位写权限未单独断言 |
| TC-MVP-07 审计开关 | **部分符合** | `audit_trail=true` 时批准链写入审计（M3 集成）；**关审计时不写事件无单测**；`PATCH /settings/features` **固定 `audit(..., True)`** 绕过开关（M3-005） |
| TC-MVP-09 备份恢复 | **部分符合** | 库 + config + reports 打包；DB `sha256` 不一致 422；**config/报告无摘要**；恢复后 **内存 `state.limits` 未重载**（M3-003、M3-004） |
| TC-MVP-13a 默认 `e_sign=false` 不阻塞 | **符合** | `FeaturesConfig` / `features.example.yaml` 默认 false；`test_esign_off_applies_immediately`；`/ready` 断言 `e_sign` false |
| TC-MVP-13b 开签名拦截限值 | **符合（全局路径）** | 409 `APPROVAL_REQUIRED`，限值保持原值直至他人批准 |
| TC-MVP-13c 批准/驳回/过期 | **符合** | 自批 403；驳回/过期不生效；批准后全局限值更新 |
| TC-MVP-17 趋势 | **基本符合** | `GET /trends?metric=` 过滤测量序列；无时间窗/聚合（MVP API 级可接受） |
| TC-MVP-21 审计只追加 | **符合（应用层）** | `Store.append_audit` 无删改 API；`DELETE /audit/{id}` → 403 `FORBIDDEN`；**DB 角色级 INSERT-only 未做**（MVP 已知差距，P2） |

## 4. 符合项（重点核对）

| 关注点 | 结论 |
|--------|------|
| `e_sign` 默认关不阻塞主流程 | **符合**：默认 false；关签名时限值 PATCH 200 且 `applied: true` |
| 开签名时限值不立刻生效 | **符合（`/settings/limits`）**：抛 `APPROVAL_REQUIRED`，`state.limits` 不变直至 `Approved` |
| 发起人不能自批 | **符合**：`decide_approval` 校验 `actor.username == requester` → 403 |
| 驳回 / 过期不生效 | **符合**：`Rejected` 不写 limits；过期 `APPROVAL_REJECTED`；`sweep_expired` 在列表/审批前执行 |
| 审计无删除 | **符合**：仅 `append_audit`；删除路由恒 403 |
| 备份含库 + 配置 + 报告 | **符合**：`create_backup` 复制 `mer.sqlite`、`_config_files`、`report_dir` |
| DB 校验失败拒绝恢复 | **符合**：`db_sha256` 不匹配 → `VALIDATION_ERROR` |
| 密钥不入库 | **符合**：见 §2 |
| RBAC Gateway 强制 | **符合**：`require(perm)` 装饰合规路由 |

## 5. 问题清单

| ID | 级别 | 位置 | 问题描述 | 依据 | 建议 |
|----|------|------|----------|------|------|
| M3-001 | **P1** | `compliance.request_limit_change` / `decide_approval`；`main.py` `state.limits` | 批准后限值仅更新 **进程内 dict**（与 `scheduler.limits` 同引用），**不写入 SQLite 也不回写 `limits.example.yaml`**。重启后限值回到 YAML 初始值，批准记录与生效状态脱节。 | DS §6.2「Approved → 限值变更落库」；URS-DAT-003/004；TC-MVP-13b/c 隐含持久化 | 增加 `limits` 配置快照表或版本化行；批准生效时持久化并在启动时加载；补「重启后限值仍为批准值」集成测。 |
| M3-002 | **P1** | `compliance.py` `patch_point` | `PATCH /points/{id}` 可携带 `limits` 等字段 **直接 `save_point`**，不经过 `e_sign` / `ApprovalRequest`。签名开启时存在 **旁路改限值** 风险。 | DS §6.2 绑定点「限值变更」；§9 签名开启强制批准流 | 点位内限值变更走同一 `request_limit_change` 或禁止 PATCH `limits`；e_sign 开时仅经批准流；补回归用例。 |
| M3-003 | **P1** | `backup.restore_backup`；`main.py` lifespan | 恢复会覆盖磁盘上 DB 与 `config/`、报告目录，但 **不重新 `load_limits` / `load_features`**，运行中 `state.limits`、`state.features` 仍为恢复前内存值，与磁盘/库不一致。 | TC-MVP-09 恢复一致性；DS §10.2 | 恢复成功后重载 limits/features 或要求进程重启并文档化；集成测：改限值→备份→破坏→恢复后 GET limits 与磁盘一致。 |
| M3-004 | **P2** | `backup.py` `manifest.json` | Manifest 仅含 **`db_sha256`**；config/报告文件篡改后 **仍会恢复**，与「校验失败拒绝恢复」字面范围不完全对齐。 | DS §10.2 + plan TC-MVP-09 | 扩展 manifest 对各文件 hash；任一项不匹配拒绝恢复；或 decisions 书面限定 MVP 仅校验 DB。 |
| M3-005 | **P2** | `compliance.py` `patch_features` L100 | `features.update` 审计调用 **`audit(..., True)`**，在 `audit_trail=false` 时仍写入审计，违背开关语义。 | DS §9 审计开关；TC-MVP-07 | 改为 `state.features.audit_trail`；补关审计时 features 变更无记录用例。 |
| M3-006 | **P2** | `doc/spec/ds.md` §9「报告批准」 | 批准流 **仅绑定 `limit.update`**；报告生成/批准未接 `e_sign`。 | DS §9 典型绑定动作 | M4 或 M3 补丁：报告批准占位 + TC；或书面豁免写入 decisions。 |
| M3-007 | **P2** | `test_m3_compliance.py` | 驳回/过期用例用 **`!= 10` / `!= 12` 弱断言**，未锚定初始 `humidity_pct.max==70`；无 **audit_trail=false** 零写入断言；无 **出厂配置** 单测（依赖 `test_gateway` 分散）。 | `spec/testing_quality.md`；plan TC-MVP-07/13a/21 | 断言精确前后值；独立 `test_audit_trail_off_no_rows`；M3 文件内断言 `features.example.yaml` 默认。 |
| M3-008 | **P2** | `backup` / `store` | DS §10.2 冻结范围为 **PostgreSQL**；实现为 **SQLite 文件备份**（与 M2 开发默认一致）。 | DS §10.2 | 部署手册区分 dev(SQLite) vs prod(PG 逻辑备份)；或 adapter 抽象备份源。 |
| M3-009 | **P2** | `ApprovalRequest` 实现 | 无 **Draft** 态，创建即 **Pending**（DS §6.2 表含 Draft）。 | DS §6.2 | MVP 可接受；文档标明单步省略 Draft。 |
| M3-010 | **P2** | `conftest` / 测试密钥 | CI 使用固定 `MER_SECRET` 与 `admin/admin` 开发用户库。 | `spec/security.md` | 保持 prod 门禁；M4 用户目录落地后弱化默认管理员故事（延续 M2-008）。 |

## 6. 与文档一致性

| 文档 | 结论 |
|------|------|
| `doc/spec/ds.md` §6.2 | **部分偏差**：批准后主路径正确；**落库/绑定点范围**不足（M3-001、M3-002、M3-006） |
| `doc/spec/ds.md` §9 | **部分偏差**：RBAC/开关/自批/口令挑战符合；审计开关一处旁路（M3-005）；DB 角色只追加未实现（P2） |
| `doc/spec/ds.md` §10.2 | **部分偏差**：范围内容齐全；引擎为 SQLite；校验仅 DB（M3-004、M3-008） |
| `ref/plan.md` TC-MVP-* | **自动化覆盖主路径**；TC-MVP-07/01/09 存在上述缺口 |

## 7. 是否可进 M4

| 项 | 建议 |
|----|------|
| 总体 | **有条件通过**：演示与 pytest 全绿，合规 **行为原型** 到位。 |
| 是否建议整改后再进 M4 | **是**：至少闭环 **M3-001～M3-003（P1）**，避免 M4 前端（批准中心、趋势、开关）建立在易失限值与恢复不一致之上。M3-004～M3-010 可与 M4 并行或书面豁免。 |

## 8. 优先整改顺序

1. **M3-001**：限值批准结果持久化 + 启动加载。
2. **M3-002**：消除点位 PATCH 签名旁路。
3. **M3-003**：恢复后配置/限值与运行态一致（热加载或强制重启契约）。
4. **M3-007**：加强测试断言与审计关断覆盖。
5. **M3-004 / M3-005 / M3-006**：按合规优先级择项纳入 M3 补丁或 decisions。

---

**返回摘要（给协调方）**

- **结论**：有条件通过  
- **P0**：无（未发现签名默认关仍强制待批、自批成功、审计可删、DB 摘要篡改仍恢复等 **阻断级逻辑错误**）  
- **P1**：M3-001、M3-002、M3-003  
- **是否建议整改后再进 M4**：**是**（至少处理 P1 三项）
