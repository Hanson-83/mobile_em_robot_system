# 代码审核整改说明（2026-09-25）

## 输入

- `ref/review/review_M1_260925.md`（有条件通过，无 P0）
- `ref/review/review_M2_260925.md`（有条件通过，P0：报告超限关联）
- `ref/review/review_M3_260925.md`（不通过）
- `ref/review/review_M4_260925.md`（不通过，P0 与 M2-001 相同）
- 复核：`ref/review/review_fix_260925.md`（整改成立，无剩余 P0）

## 已改

| 原编号 | 处理 |
|--------|------|
| M2-001 / M4-001 | 报告按任务测量 ID 与 `object_ref=task_id` 关联报警；测例断言 `breach_count` 与 HTML |
| M2-002 | `wait` 技能按秒等待 |
| M2-004 / M2-005 / M2-006 | 补默认排队互斥、故障码、Running 断线失败测例 |
| M2-007 | 测量补传成功且非回放时做限值评估 |
| M2-008 | 离线失败报警规则统一为 `device.offline` |
| M3-001 | 用户组绑定角色并计入登录权限；可 PATCH 组与角色权限 |
| M3-002 | `auto_backup_interval_minutes` 默认 60，进程启动按间隔自动备份 |
| M3-003 | 报告直接批准写审计；关闭审计开关前先记一笔 |
| M3-004 | 备份清单记录活动配置路径，恢复写回该路径 |
| M3-005 | SQLite 触发器禁止改删审计；PostgreSQL 脚本 `deploy/sql/audit_append_only.sql` |
| M3-006 | 批准、签名、限值或报告生效同一事务 |
| M3-008 | 备份目录名含微秒与随机后缀 |
| M1-001 / M1-002 | OpenAPI 声明 Bearer，并写出 401/403/422/429 领域错误 |
| M1-003 | 无效 Bearer 与匿名请求共用 IP 限流桶 |
| M1-004 / M4-002 | WebSocket 订阅后持续推送并有心跳；地图页发送订阅；任务/报警/趋势页定时刷新 |
| M1-006 | 适配器可配置心跳超时，超时后 `heartbeat()` 视为重连；默认 0 不启用 |

## 未改行为、留给确认

| 原编号 | 原因 |
|--------|------|
| M2-003 | MVP 仍省略 Created / Dispatched。见待确认 C-17 |
| M4-003 的历史浏览器范围 | 早先走查未覆盖用户页与批准中心。报警、用户、批准以 API 测试为主。整改后的开关页需再点一次 |

## 复核

Reviewer_Fix 对上述修复做了代码核对，并复跑相关测试 26 通过，另用临时客户端确认 WebSocket 在订阅后能收到新测量事件。结论：整改成立，无剩余 P0。
