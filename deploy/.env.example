# 复制为 deploy/.env 后按现场修改。不要提交真实密钥。
MER_SECRET_KEY=replace-with-long-random-string
MER_ALLOW_DEV_SECRET=false
MER_DEV_SEED=false
MER_ENV=production
# 生产数据库示例（需安装 backend 的 postgres 可选依赖并取消 compose profile）
# MER_DATABASE_URL=postgresql+psycopg://mer:replace-me@postgres:5432/mer
