# MER_SECRET 必须由运行环境注入（≥16 字符，禁止 change-me-dev-only）
MER_ENV=dev
MER_SECRET=
MER_ALLOW_DEV_AUTH=false
# 仅当 MER_ALLOW_DEV_AUTH=true 时生效，JSON 用户库，勿提交真实口令
# MER_DEV_USERS_JSON=
GATEWAY_RATE_LIMIT_PER_MIN=60
CORS_ORIGINS=http://localhost:5173
DATABASE_URL=sqlite+pysqlite:///:memory:
